{
  "name": "vancrox-frontend",
  "version": "1.0.0",
  "description": "VANCROX Crypto Investment Platform — Frontend",
  "scripts": {
    "start": "npx serve . -p 3000",
    "dev": "npx live-server --port=3000 --open=login.html",
    "preview": "npx serve . -p 3000 --no-clipboard"
  },
  "devDependencies": {
    "live-server": "^1.2.2",
    "serve": "^14.2.1"
  },
  "homepage": "./",
  "engines": {
    "node": ">=16.0.0"
  }
}
